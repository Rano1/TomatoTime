<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/listview_bg"
    android:orientation="vertical" >

    <RelativeLayout
        android:id="@+id/layout_row"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="10.0dip"
        android:paddingLeft="10.0dip"
        android:paddingTop="10.0dip" >

        <CheckBox
            android:id="@+id/checkBox_cir"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_gravity="left"
            android:button="@drawable/cir_checkbox"
            android:checked="false"
            android:focusable="false"
            android:focusableInTouchMode="false"
            android:paddingLeft="5.0dip" />

        <CheckBox
            android:id="@+id/checkBox_pin"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:button="@drawable/pin_checkbox"
            android:checked="false"
            android:focusable="false"
            android:focusableInTouchMode="false"
            android:paddingRight="10.0dip" />

        <TextView
            android:id="@+id/tv_list_item_content"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="false"
            android:layout_centerVertical="true"
            android:layout_marginLeft="5.0dip"
            android:layout_toLeftOf="@id/checkBox_pin"
            android:layout_toRightOf="@id/checkBox_cir"
            android:background="@android:color/transparent"
            android:longClickable="false"
            android:text="New TextView"
            android:textSize="16.0sp" />
    </RelativeLayout>

    <View
        android:id="@+id/important_color"
        android:layout_width="3.0dip"
        android:layout_height="0.0dip"
        android:layout_alignBottom="@id/layout_row"
        android:layout_alignTop="@id/layout_row"
        android:background="#ffff0000" />

    <Button
        android:id="@+id/btn_pin"
        android:layout_width="50.0dip"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@id/layout_row"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@id/layout_row"
        android:layout_centerVertical="true"
        android:background="@android:color/transparent"
        android:focusable="false"
        android:focusableInTouchMode="false" />

</RelativeLayout>